/**
 * @param {string} s
 * @param {string} p
 * @return {boolean}
 */
var isMatch = function(s, p) {
    let i = 0;
    let j = 0;
    const lens = s.length;
    const lenp = p.length;
    let flag = true;
    while (flag) {
        if (p[j] !== '*' && p[j] !== '.') {
            
        }
    }
};