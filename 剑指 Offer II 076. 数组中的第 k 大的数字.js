/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number}
 */

// 快排 todo
var findKthLargest = function (nums, k) {
    const quickSort = arr => {
        if (arr.length === 1) {
            return arr;
        }
    };
};
